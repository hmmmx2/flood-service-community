global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'floodwatch-backend'
    static_configs:
      - targets: ['flood-service-community-production.up.railway.app']
    scheme: https
    metrics_path: '/actuator/prometheus'
    tls_config:
      insecure_skip_verify: true